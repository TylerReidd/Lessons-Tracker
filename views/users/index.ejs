<%- include('../partials/header') %>
<body>
  <div class="navbar-fixed">
    
  </div>
  <ul>Registered Users:
    <% users.forEach(u => { %>
      <a href="/users<%= u._id %>">
      <%= u.name %></a> - Joined on <% u.createdAt.toLocaleString()
      .split(',')[0] %>
    <% }) %>
  </ul>
</body>

</html>